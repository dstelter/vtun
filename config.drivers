#!/bin/sh
#
# Create drivers and protocols links
#
case $1 in
   link)
	for i in `cat vtun.drivers`; do
	   if test -f $2/$i -a ! -f $i; then
	      ln -f -s $2/$i $i
	   fi
	done 
        ;;
   unlink)
	for i in `cat vtun.drivers`; do
	   rm -f $i
	done 
        ;;
esac
